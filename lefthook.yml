pre-commit:
  commands:
    biome:
      glob: '*.{js,ts,jsx,tsx}'
      env:
        # Cursor Cloud 等では node が /exec-daemon にあるため含める
        PATH: /opt/homebrew/bin:/usr/local/bin:/exec-daemon:/usr/bin:/bin
      run: node_modules/.bin/biome check --write {staged_files}
      stage_fixed: true

