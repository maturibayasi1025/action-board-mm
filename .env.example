# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=service-role-key
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000

SITE_URL=http://localhost:3000
ADDITIONAL_REDIRECT_URLS=http://localhost:3000

# 開発時は空でもかまいません
NEXT_PUBLIC_SENTRY_DSN=your-project-dsn
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development

ENABLE_CONFIRMATIONS=true

# ローカル開発ではSTMP_ENABLEDをfalseとすることで、Inbucketにメールが届くようになる
# 本番・STG環境ではSMTP_ENABLEDをtrueとし、それ以外の項目についても設定が必要
SMTP_ENABLED=false
SMTP_HOST=smtp.mailgun.org
SMTP_USER=example@mailgun.org
SMTP_PASS=password
SMTP_ADMIN_EMAIL=noreply@example.com
SMTP_SENDER_NAME=test_sender

# Mailgun
MAILGUN_API_KEY=mailgun-api-key
MAILGUN_DOMAIN=team-mir.ai

# バッチ処理API用の管理者キー（本番環境では強力なランダム文字列を設定）
BATCH_ADMIN_KEY=development-admin-key-2025

# アンケート初回回答後に案内するグッジョブ（missions.id）。未設定時は案内なし
# SURVEY_ENPS_POST_MISSION_ID=
# SURVEY_AWARD_POST_MISSION_ID=

# LINE Login credentials (Get from LINE Developers Console)
LINE_CLIENT_SECRET=your-line-channel-secret
NEXT_PUBLIC_LINE_CLIENT_ID=your-line-channel-id

# HubSpot API連携用のアクセストークン
HUBSPOT_API_KEY=pat-hubspot-api-key
HUBSPOT_CONTACT_LIST_ID=1

# データ基盤用
BQ_USER_PASSWORD=bq-password

# Slack Incoming Webhooks（グッジョブいいね・ログ等。アンケート系のフォールバックにも使用）
SLACK_WEBHOOK_URL=your-slack-webhook-url
# eNPS / 表彰アンケート専用（未設定時は SLACK_WEBHOOK_URL にフォールバック）
# SLACK_WEBHOOK_URL_ENPS=https://hooks.slack.com/services/...
# SLACK_WEBHOOK_URL_AWARD=https://hooks.slack.com/services/...
# Slack Bot User OAuth Token（users.list のページングで全メンバー取得し、表示名→メンションID解決。グッジョブ通知・未回答メンションに使用）
# Slack アプリの OAuth で Bot Token Scopes に users:read を付与すること。本番（Vercel）の /api/slack-notification は Edge のため環境変数必須。
SLACK_BOT_TOKEN=xoxb-your-bot-token
